#include <stdio.h>
#include "holberton.h"
/**
 *main - value of an integer
 *Return: 0
 */
int main(void)
{
int sum = 0;
int ite;
for (ite = 1; a < 1024; a++)
{
if ((ite % 3 == 0 || (ite % 5 == 0))
{
num = num + ite;
}
}
printf("%d\n", sum);
return (0);
}
